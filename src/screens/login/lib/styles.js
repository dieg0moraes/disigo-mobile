import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    header: {
      fontSize: 40,
      marginBottom: 100
    },

    input: {
      width: '60%',
      height: 50,
      margin: 5,
      borderRadius: 20,
      backgroundColor: 'white',
      textAlign: 'center',
    }
});
/*
 *
 */
export {
    styles
}
